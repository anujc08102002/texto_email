NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Platform admin console (server-side only — not exposed to the browser)
ADMIN_EMAIL=admin@texto.local
ADMIN_PASSWORD=AdminTexto1!

# Razorpay TEST keys live on the backend (.env). The UI loads public keyId from GET /api/v1/billing/config.
